<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <%- include('includes/nav.ejs') %>

    <% if (users.length > 0) { %>
        <ul>
            <% for(let user of users) { %>
                <li><%= user %></li>
            <% } %>
        </ul>
    <% } else { %>
        <h1>No users yet! Make one by clicking <a href="/">here</a>!</h1>
    <% } %>
</body>
</html>